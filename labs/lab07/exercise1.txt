Scenario 1
    1.Because step size in bytes is exactly equal to cache size in bytes.
    2. 0.0
    3.Sample answer (choose one): step size, 1

Scenario 2
    1. 3 memory accesses
    2.Shortest repeating pattern : MHHH
    3.Hit rate as Rep Count approaches infinity, decimal value
    Decimal: 0.75

Scenario 3
    1.[0.50], [0.00], [0.50]
    2.[32], [16]
    3.16
    4.rep count
    5.+,+,+,+

--- lines below are ignored by the AG ---

Checkoff Question 1:Every memory access maps to the single cache set Set 0. 
                    Each new write evicts the previous block stored in Set 0, so there are never any cache hits, no matter how many repetitions we run.
Checkoff Question 2:step size, array size
Checkoff Question 3:Explanation: Cache block holds 4 int words (16B). Step size=2 accesses word0 then word2 in the same block. 
                    First word triggers a compulsory miss (MH), second word is fully cached (HH). 
                    Combined repeating sequence MHHH, 3 hits out of 4 total accesses → hit rate 0.75.
Checkoff Question 4:Reason: The repeating MHHH pattern persists infinitely with no cache evictions. 
                    The long-run average hit rate stays exactly 3/4 = 0.75.
Checkoff Question 5:We should use tiling (chunking). Instead of applying one function to the entire large array before moving to the next function, we process one chunk of the array at a time. 
                    We execute all functions on the current chunk fully before moving to the next chunk. 
                    This keeps the chunk’s data resident in cache and improves temporal locality, achieving high hit rates.
